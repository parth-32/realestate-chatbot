# OpenRouter API Key (Optional: Can also be set in the Web UI Settings)
OPENROUTER_API_KEY=

# Default Free Model
DEFAULT_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Backend URL for Next.js internal proxy (in docker-compose this points to http://backend:8000)
BACKEND_URL=http://backend:8000
